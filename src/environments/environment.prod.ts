// ── Environment configuration for Production ─────────────────────────────────
// Use this file for production build settings

export const environment = {
    production: true,
    baseUrl: '/api/',
    apiEndpoint: 'groups/list'
};
