// ── Environment configuration for Development ─────────────────────────────────
// Use this file for local development settings

export const environment = {
    production: false,
    baseUrl: 'http://localhost:3000/api/',
    apiEndpoint: 'groups/list'
};
